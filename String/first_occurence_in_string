class Solution {
public:
    int strStr(string haystack, string needle) {
        int index = -1;

        index = haystack.find(needle) ;

        if(index == -1) return -1 ;
        else return index ;

    }
};